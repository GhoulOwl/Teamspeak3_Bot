# 音频控制器

<cite>
**本文引用的文件**
- [controller.py](file://bot/core/audio/controller.py)
- [ffmpeg.py](file://bot/core/audio/ffmpeg.py)
- [volume.py](file://bot/core/audio/volume.py)
- [music.py](file://bot/core/commands/handlers/music.py)
- [app.py](file://bot/app.py)
- [config.py](file://bot/config.py)
- [config.yaml](file://config/config.yaml)
- [default.pa](file://docker/pulseaudio/default.pa)
- [init_identity.py](file://docker/ts3client/init_identity.py)
- [entrypoint.sh](file://docker/entrypoint.sh)
</cite>

## 更新摘要
**变更内容**
- **双sink分离架构**：从单sink双用途（ts3bot_sink）迁移到双sink分离架构（ts3bot_music + ts3bot_playback），完全消除音频反馈循环问题
- **音频质量优化**：更新FFmpeg音频滤镜链参数，从150ms分析窗口和0.95峰值目标优化为500ms分析窗口和0.9峰值目标，提升音频质量
- **改进的音频隔离**：通过两个独立的PulseAudio sink实现音乐输出和回放音频的完全分离
- **增强的音频路由稳定性**：新的架构确保音乐播放不会被回放音频干扰，避免音频循环问题

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [双sink分离架构](#双sink分离架构)
7. [音频质量优化](#音频质量优化)
8. [音频隔离与反馈循环消除](#音频隔离与反馈循环消除)
9. [跨平台音频处理](#跨平台音频处理)
10. [音频质量验证系统](#音频质量验证系统)
11. [PulseAudio路由机制改进](#pulseaudio路由机制改进)
12. [持续时间参数与提前退出检测](#持续时间参数与提前退出检测)
13. [临时文件管理与自动清理](#临时文件管理与自动清理)
14. [依赖关系分析](#依赖关系分析)
15. [性能考虑](#性能考虑)
16. [故障排查指南](#故障排查指南)
17. [结论](#结论)
18. [附录](#附录)

## 简介
本技术文档围绕音频控制器模块展开，重点解析 AudioController 的架构设计与实现原理，涵盖：
- 播放状态机（IDLE、PLAYING、PAUSED）的状态转换逻辑
- 播放控制方法（play、stop、pause、resume）的实现机制与异步处理流程
- 回调函数系统的设计：播放停止与错误处理回调的注册与触发机制
- 音频播放生命周期的完整管理：播放开始、结束与异常处理
- **新增**：双sink分离架构，实现音乐输出与回放音频的完全隔离
- **新增**：音频质量优化，提升动态音频规范化效果
- **新增**：改进的音频隔离机制，完全消除音频反馈循环问题

## 项目结构
音频控制器位于 bot/core/audio 目录，配合 FFmpeg 子进程管理和 PulseAudio 实时音量控制，形成完整的播放链路。系统现已支持双sink分离架构，通过 ts3bot_music 和 ts3bot_playback 两个独立的PulseAudio sink 实现音频隔离。命令层通过 BotApplication 将控制器与音乐队列、网络服务等模块集成。

```mermaid
graph TB
subgraph "音频子系统"
AC["AudioController<br/>状态机与播放控制<br/>双sink架构支持"]
FFM["FFmpegProcess<br/>子进程管理<br/>动态音频规范化优化<br/>Linux: ts3bot_music sink"]
VOL["VolumeController<br/>音量与淡入淡出<br/>Linux: pactl实时控制"]
end
subgraph "命令与应用"
APP["BotApplication<br/>应用编排"]
CMD["music.py 命令处理器"]
CTX["CommandContext<br/>命令上下文"]
end
subgraph "服务层"
YTDL["YtDlpService<br/>音频下载与缓存"]
CACHE["TTLCache<br/>内存缓存管理"]
end
subgraph "配置与硬件"
CFG["config.py<br/>音频参数"]
YAML["config.yaml<br/>配置文件"]
PA["default.pa<br/>双sink分离配置"]
TS3["init_identity.py<br/>TS3客户端音频设置"]
ENDPT["entrypoint.sh<br/>PulseAudio启动脚本"]
end
AC --> FFM
AC --> VOL
APP --> AC
APP --> CMD
CMD --> AC
CMD --> YTDL
YTDL --> CACHE
CTX --> CMD
APP --> CFG
CFG --> YAML
YAML --> PA
PA --> TS3
TS3 --> ENDPT
```

**图表来源**
- [controller.py:24-187](file://bot/core/audio/controller.py#L24-L187)
- [ffmpeg.py:18-23](file://bot/core/audio/ffmpeg.py#L18-L23)
- [volume.py:13-20](file://bot/core/audio/volume.py#L13-L20)
- [music.py:17-260](file://bot/core/commands/handlers/music.py#L17-L260)
- [app.py:27-348](file://bot/app.py#L27-L348)
- [config.py:48-55](file://bot/config.py#L48-L55)
- [config.yaml:14-18](file://config/config.yaml#L14-L18)
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)
- [entrypoint.sh:38-79](file://docker/entrypoint.sh#L38-L79)

## 核心组件
- **AudioController**：高层播放控制器，负责状态机、播放生命周期、回调事件分发、音量控制与 FFmpeg 进程协调。现已支持双sink架构和改进的音频质量控制。
- **FFmpegProcess**：封装 FFmpeg 子进程的启动、停止、暂停/恢复、标准错误监控与退出码处理。使用优化的动态音频规范化滤镜链，支持 ts3bot_music sink 输出。
- **VolumeController**：两层音量控制策略（FFmpeg 滤镜粗调 + pactl 实时微调）。Linux 支持精细控制，macOS 仅支持 FFmpeg 粗调。
- **YtDlpService**：音频下载服务，支持多种平台的音频提取和本地文件下载，生成包含持续时间信息的临时文件用于播放。
- **TTLCache**：内存缓存管理，提供带TTL的键值存储，用于缓存下载结果和元数据。
- **BotApplication**：应用编排器，注入 AudioController 并注册播放完成/错误回调，驱动音乐队列自动播放下一首。
- **命令处理器 music.py**：将用户指令映射到 AudioController 的播放控制方法，并与队列联动。
- **CommandContext**：命令上下文封装，提供回复 TS3 的便捷方法。
- **config.py**：音频相关配置项，如 PulseAudio 源名、默认音量、淡入时长、FFmpeg 路径等。
- **config.yaml**：音频配置文件，定义 pulse_sink_name、default_volume、fade_duration_ms 等参数。
- **default.pa**：PulseAudio 配置文件，创建双sink分离架构（ts3bot_music 和 ts3bot_playback）。
- **init_identity.py**：TS3客户端音频设置，配置双sink架构的音频设备。
- **entrypoint.sh**：PulseAudio启动脚本，确保正确的音频路由配置。

**章节来源**
- [controller.py:24-187](file://bot/core/audio/controller.py#L24-L187)
- [ffmpeg.py:18-23](file://bot/core/audio/ffmpeg.py#L18-L23)
- [volume.py:13-20](file://bot/core/audio/volume.py#L13-L20)
- [music.py:17-260](file://bot/core/commands/handlers/music.py#L17-L260)
- [app.py:27-348](file://bot/app.py#L27-L348)
- [config.py:48-55](file://bot/config.py#L48-L55)
- [config.yaml:14-18](file://config/config.yaml#L14-L18)
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)
- [entrypoint.sh:38-79](file://docker/entrypoint.sh#L38-L79)

## 架构总览
AudioController 作为门面，向上提供统一的播放接口，向下协调 FFmpeg 子进程与音量控制器。系统采用双sink分离架构，通过 ts3bot_music sink 输出音乐，ts3bot_playback sink 接收其他用户的音频回放，实现完全的音频隔离。BotApplication 注册播放完成与错误回调，实现"一首歌结束后自动播放下一首"的自动化播放链路。

```mermaid
sequenceDiagram
participant User as "用户"
participant Cmd as "music.py 命令处理器"
participant App as "BotApplication"
participant Ctrl as "AudioController"
participant YTDL as "YtDlpService"
participant Vol as "VolumeController"
participant FF as "FFmpegProcess"
User->>Cmd : "!play ..."/"!pause"/"!resume"/"!stop"
Cmd->>YTDL : 下载音频到临时文件
YTDL-->>Cmd : 返回包含持续时间的临时文件路径
Cmd->>Ctrl : 调用 play(source, temp_file, duration)
Ctrl->>FF : 启动 FFmpeg 播放ts3bot_music sink
alt Linux
Ctrl->>Ctrl : 播放前延迟 0.5秒
Ctrl->>Vol : 刷新音量输入/设置音量
else macOS
Ctrl->>Ctrl : 跳过音量输入刷新
end
FF-->>Ctrl : EOF 或 错误事件
Ctrl->>Ctrl : 清理临时文件
Ctrl-->>App : 触发 on_stopped/on_error 回调
App->>Ctrl : 自动播放下一首若队列存在
```

**图表来源**
- [music.py:57-63](file://bot/core/commands/handlers/music.py#L57-L63)
- [controller.py:76-187](file://bot/core/audio/controller.py#L76-L187)
- [ffmpeg.py:98-113](file://bot/core/audio/ffmpeg.py#L98-L113)
- [ffmpeg.py:268-303](file://bot/core/audio/ffmpeg.py#L268-L303)
- [volume.py:77-120](file://bot/core/audio/volume.py#L77-L120)

## 详细组件分析

### AudioController 状态机与播放控制
- **状态枚举 PlaybackState**：IDLE、PLAYING、PAUSED
- **状态转换**：
  - play：若非 IDLE 先 stop；启动 FFmpeg；进入 PLAYING；**Linux 平台**：稍后刷新音量输入
  - pause：仅 PLAYING 可暂停，进入 PAUSED
  - resume：仅 PAUSED 可继续，进入 PLAYING
  - stop：停止 FFmpeg，进入 IDLE，清理临时文件
  - EOF/错误：进入 IDLE 并触发 on_stopped/on_error 回调，清理临时文件
- **异步处理**：所有控制方法均为协程，确保非阻塞；**Linux 平台**：播放开始后短暂延时再刷新音量输入，避免 PulseAudio 未就绪导致的失败
- **持续时间参数**：play() 方法新增 duration 参数，用于预期播放时长检测
- **自动清理机制**：所有状态转换都会自动清理临时文件，确保资源及时释放
- **双sink支持**：AudioController 现在支持 ts3bot_music sink，为双sink架构做准备

**更新**：新增双sink架构支持和改进的音频质量控制

```mermaid
stateDiagram-v2
[*] --> IDLE
IDLE --> PLAYING : "play(source, temp_file, duration)"
PLAYING --> PAUSED : "pause()"
PAUSED --> PLAYING : "resume()"
PLAYING --> IDLE : "stop()/EOF/错误"
PAUSED --> IDLE : "stop()"
note right of IDLE : "平台检测 : platform.system() == 'Darwin'<br/>自动清理 : _cleanup_temp_file()<br/>持续时间检测 : expected_duration<br/>双sink支持 : ts3bot_music"
note right of PLAYING : "Linux : 播放前延迟 0.5秒<br/>刷新音量输入<br/>macOS : 跳过刷新<br/>动态音频规范化优化<br/>500ms分析窗口<br/>0.9峰值目标"
```

**图表来源**
- [controller.py:18-24](file://bot/core/audio/controller.py#L18-L24)
- [controller.py:76-118](file://bot/core/audio/controller.py#L76-L118)
- [controller.py:119-187](file://bot/core/audio/controller.py#L119-L187)

**章节来源**
- [controller.py:18-24](file://bot/core/audio/controller.py#L18-L24)
- [controller.py:76-118](file://bot/core/audio/controller.py#L76-L118)
- [controller.py:119-187](file://bot/core/audio/controller.py#L119-L187)

### FFmpegProcess 子进程管理
- **启动**：构建 FFmpeg 命令（重连参数、音量滤镜、动态音频规范化优化），创建子进程并启动监控任务
- **停止**：取消监控任务；终止进程，超时则强制 kill；清理内部状态
- **暂停/恢复**：向进程发送 SIGSTOP/SIGCONT
- **监控**：读取 stderr 日志，区分 EOF（正常结束）、SIGSTOP（暂停）、其他错误码并触发回调
- **平台差异**：
  - Linux：使用 `-f pulse` 输出到 ts3bot_music sink
  - macOS：使用 `-f audiotoolbox` 输出到系统 Core Audio
- **持续时间检测**：新增基于预期时长的提前退出检测逻辑
- **音频质量优化**：动态音频规范化滤镜链参数优化，提升音频质量

**更新**：音频滤镜链参数优化，从150ms分析窗口和0.95峰值目标优化为500ms分析窗口和0.9峰值目标

```mermaid
flowchart TD
Start(["start(url, volume, expected_duration)"]) --> CheckRunning{"是否已在运行？"}
CheckRunning --> |是| StopPrev["stop() 停止旧进程"]
CheckRunning --> |否| BuildCmd["构建 FFmpeg 命令"]
BuildCmd --> Platform{"平台检测"}
Platform --> |Linux| LinuxCmd["添加 -f pulse<br/>-sink_name ts3bot_music<br/>动态音频规范化优化"]
Platform --> |macOS| MacCmd["添加 -f audiotoolbox<br/>default"]
LinuxCmd --> Spawn["创建子进程并忽略 stdout"]
MacCmd --> Spawn
Spawn --> Monitor["启动监控任务读取 stderr"]
Monitor --> CheckPremature{"检查提前退出"}
CheckPremature --> |是| LogWarning["记录警告：提前退出"]
CheckPremature --> |否| NormalEnd["正常结束"]
NormalEnd --> End(["等待事件"])
Stop(["stop()"]) --> CancelTask["取消监控任务"]
CancelTask --> Terminate["尝试 terminate()"]
Terminate --> Wait{"等待返回码"}
Wait --> |超时| Kill["kill() 强制结束"]
Wait --> |正常| Done(["完成"])
```

**图表来源**
- [ffmpeg.py:98-113](file://bot/core/audio/ffmpeg.py#L98-L113)
- [ffmpeg.py:223-303](file://bot/core/audio/ffmpeg.py#L223-L303)
- [ffmpeg.py:268-273](file://bot/core/audio/ffmpeg.py#L268-L273)

**章节来源**
- [ffmpeg.py:18-23](file://bot/core/audio/ffmpeg.py#L18-L23)
- [ffmpeg.py:98-113](file://bot/core/audio/ffmpeg.py#L98-L113)
- [ffmpeg.py:223-303](file://bot/core/audio/ffmpeg.py#L223-L303)

### VolumeController 音量控制与淡入淡出
- **两层控制**：
  - FFmpeg 层：启动时设置音量滤镜，提供粗略音量
  - PulseAudio 层：通过 pactl 对 sink input 实时调整，实现平滑过渡（Linux 专用）
- **平台差异**：
  - Linux：支持两层控制，包括实时音量调整
  - macOS：仅支持 FFmpeg 粗调，无实时音量控制
- **刷新 sink 输入**：启动 FFmpeg 后扫描当前 PulseAudio sink 的 sink-input，定位并绑定实时音量控制
- **淡入淡出**：按固定步长与时间间隔逐步调整，避免突变噪声
- **双sink兼容**：支持 ts3bot_music sink 的音量控制

**章节来源**
- [volume.py:13-20](file://bot/core/audio/volume.py#L13-L20)
- [volume.py:32-52](file://bot/core/audio/volume.py#L32-L52)
- [volume.py:84-120](file://bot/core/audio/volume.py#L84-L120)

### 回调系统与播放生命周期
- **回调类型 PlaybackCallback**：无参协程函数
- **注册**：BotApplication 在构造时调用 AudioController.set_callbacks(on_stopped, on_error)
- **触发**：
  - EOF：AudioController._handle_eof 设置状态为 IDLE 并调用 on_stopped，清理临时文件
  - 错误：AudioController._handle_error 设置状态为 IDLE 并调用 on_error，清理临时文件
- **生命周期**：
  - 开始：play() 启动 FFmpeg，随后刷新音量输入（Linux 专用）
  - 结束：EOF 触发 on_stopped，BotApplication 自动播放下一首，清理临时文件
  - 异常：错误码触发 on_error，BotApplication 尝试下一首，清理临时文件

```mermaid
sequenceDiagram
participant Ctrl as "AudioController"
participant FF as "FFmpegProcess"
participant App as "BotApplication"
Ctrl->>FF : start(url, volume, expected_duration)
alt Linux
FF-->>Ctrl : EOF 或 错误
Ctrl->>Ctrl : 状态=IDLE
Ctrl->>Ctrl : 播放前延迟 0.5秒
Ctrl->>Ctrl : 刷新音量输入
Ctrl->>Ctrl : 清理临时文件
Ctrl-->>App : on_stopped() 或 on_error()
App->>Ctrl : 自动播放下一首
else macOS
FF-->>Ctrl : EOF 或 错误
Ctrl->>Ctrl : 状态=IDLE
Ctrl->>Ctrl : 清理临时文件
Ctrl-->>App : on_stopped() 或 on_error()
App->>Ctrl : 自动播放下一首
end
```

**图表来源**
- [controller.py:161-187](file://bot/core/audio/controller.py#L161-L187)
- [app.py:199-253](file://bot/app.py#L199-L253)

**章节来源**
- [controller.py:61-68](file://bot/core/audio/controller.py#L61-L68)
- [controller.py:161-187](file://bot/core/audio/controller.py#L161-L187)
- [app.py:107-110](file://bot/app.py#L107-L110)

### 命令层与应用编排
- **music.py**：将 "!pause"、"!resume"、"!stop" 等命令映射到 AudioController 的对应方法；当处于 idle 时立即播放队列首曲；支持包含持续时间信息的临时文件播放
- **app.py**：在 BotApplication.__init__ 中注册回调；在 stop() 时执行 fade_out_and_stop() 安全关机
- **context.py**：CommandContext 提供 reply/reply_channel/reply_same 等便捷方法

```mermaid
classDiagram
class BotApplication {
+audio : AudioController
+music_queue : MusicQueue
+netease : NeteaseAPIClient
+start()
+stop()
-_on_playback_stopped()
-_on_playback_error()
}
class AudioController {
+state : PlaybackState
+play(url, temp_file, duration)
+stop()
+pause()
+resume()
+set_volume(volume)
+fade_out_and_stop()
+set_callbacks(on_stopped, on_error)
}
class FFmpegProcess {
+start(url, volume, expected_duration)
+stop()
+pause()
+resume()
+is_running : bool
}
class VolumeController {
+volume : int
+set_volume(target, fade_ms)
+refresh_sink_input()
}
class YtDlpService {
+download_audio(url, cache_dir)
+extract_audio(url)
}
BotApplication --> AudioController : "持有"
AudioController --> FFmpegProcess : "使用"
AudioController --> VolumeController : "使用"
BotApplication --> YtDlpService : "使用"
```

**图表来源**
- [app.py:27-110](file://bot/app.py#L27-L110)
- [controller.py:24-52](file://bot/core/audio/controller.py#L24-L52)
- [ffmpeg.py:16-44](file://bot/core/audio/ffmpeg.py#L16-L44)
- [volume.py:12-29](file://bot/core/audio/volume.py#L12-L29)

**章节来源**
- [music.py:116-136](file://bot/core/commands/handlers/music.py#L116-L136)
- [app.py:107-110](file://bot/app.py#L107-L110)

## 双sink分离架构

### 架构概述
系统已从单sink双用途（ts3bot_sink）迁移到双sink分离架构（ts3bot_music + ts3bot_playback），完全消除音频反馈循环问题：

- **ts3bot_music sink**：专门用于FFmpeg音乐输出，TS3客户端从其监视器捕获音频
- **ts3bot_playback sink**：专门用于TS3客户端播放其他用户的音频回放，不被机器人捕获
- **音频隔离**：两个sink完全分离，确保音乐播放不会被回放音频干扰

### 架构优势
- **消除反馈循环**：回放音频不会被机器人捕获，完全避免音频循环问题
- **音频质量提升**：音乐输出和回放音频分离，减少相互干扰
- **稳定性增强**：音频路由更加可靠，避免单点故障影响整个系统
- **灵活性提高**：可以独立控制音乐输出和回放音频的音量

### 配置详解
```mermaid
flowchart TD
Start["系统启动"] --> PulseAudio["启动 PulseAudio 服务"]
PulseAudio --> Config["加载 default.pa 配置"]
Config --> CreateMusicSink["创建 ts3bot_music sink<br/>rate=48000, format=s16le, channels=2"]
CreateMusicSink --> CreatePlaybackSink["创建 ts3bot_playback sink<br/>rate=48000, format=s16le, channels=2"]
CreatePlaybackSink --> SetDefaults["设置默认设备<br/>默认sink: ts3bot_music<br/>默认source: ts3bot_music.monitor"]
SetDefaults --> TS3Config["配置 TS3 客户端音频设置<br/>Capture: ts3bot_music.monitor<br/>Playback: ts3bot_playback"]
TS3Config --> BotAudio["配置 Bot 音频输出"]
BotAudio --> Route["建立完整的音频路由链路"]
Route --> Test["测试音频输出"]
Test --> Success["音频路由正常"]
```

**图表来源**
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)

### 配置文件分析
- **default.pa**：创建两个独立的null-sink模块，设置默认sink和source，配置本地协议
- **init_identity.py**：配置TS3客户端使用双sink架构，捕获从 ts3bot_music.monitor 输出的音频
- **entrypoint.sh**：确保PulseAudio服务正确启动和配置

**章节来源**
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)
- [entrypoint.sh:38-79](file://docker/entrypoint.sh#L38-L79)

## 音频质量优化

### 动态音频规范化优化
FFmpeg音频滤镜链已从150ms分析窗口和0.95峰值目标优化为500ms分析窗口和0.9峰值目标：

- **分析窗口**：从150ms增加到500ms，提供更平滑的音频动态控制
- **峰值目标**：从0.95降低到0.9，提供更好的动态范围保持
- **音频质量**：更长的分析窗口减少音频压缩感，更合理的峰值目标避免过度动态处理

### 滤镜链参数详解
```mermaid
flowchart TD
AF["音频滤镜链"] --> Volume["volume={gain}<br/>粗调音量控制"]
Volume --> Dynaudnorm["dynaudnorm<br/>f=500 : 500ms分析窗口<br/>g=15 : 高斯平滑窗口<br/>p=0.9 : 90%峰值目标"]
Dynaudnorm --> Output["输出到 ts3bot_music sink"]
```

**图表来源**
- [ffmpeg.py:147-154](file://bot/core/audio/ffmpeg.py#L147-L154)

### 优化效果
- **平滑度提升**：500ms分析窗口提供更平滑的音频动态控制
- **动态范围保持**：0.9峰值目标更好地保持音频的自然动态范围
- **音频质量改善**：减少音频压缩感，提供更自然的听觉体验
- **稳定性增强**：更长的分析窗口减少音频波动，提升播放稳定性

**章节来源**
- [ffmpeg.py:147-154](file://bot/core/audio/ffmpeg.py#L147-L154)

## 音频隔离与反馈循环消除

### 反馈循环问题
在单sink双用途架构中，FFmpeg音乐输出和TS3客户端回放音频都使用同一个sink，可能导致以下问题：
- **音频循环**：回放音频被TS3捕获，形成音频循环
- **音质下降**：相互干扰导致音频质量下降
- **系统不稳定**：音频反馈可能导致系统不稳定

### 双sink解决方案
双sink架构通过以下方式解决这些问题：
- **物理分离**：ts3bot_music sink 专门用于音乐输出，ts3bot_playback sink 专门用于回放音频
- **逻辑隔离**：TS3客户端只能从 ts3bot_music.monitor 捕获音频，无法捕获 ts3bot_playback sink 的音频
- **完全隔离**：两个sink之间没有音频路由，确保完全的音频隔离

### 音频路由图
```mermaid
flowchart LR
subgraph "音频源"
Internet["网络音频流"]
Local["本地音频文件"]
end
subgraph "音乐输出"
FFmpeg["FFmpeg 音乐播放器"]
MusicSink["ts3bot_music sink"]
Monitor["ts3bot_music.monitor"]
TS3Capture["TS3 客户端捕获"]
end
subgraph "回放音频"
TS3Client["TS3 客户端"]
PlaybackSink["ts3bot_playback sink"]
end
Internet --> FFmpeg
Local --> FFmpeg
FFmpeg --> MusicSink
MusicSink --> Monitor
Monitor --> TS3Capture
TS3Client --> PlaybackSink
PlaybackSink -.->|"无音频路由"| MusicSink
```

**图表来源**
- [default.pa:7-20](file://docker/pulseaudio/default.pa#L7-L20)
- [init_identity.py:180-184](file://docker/ts3client/init_identity.py#L180-L184)

**章节来源**
- [default.pa:7-20](file://docker/pulseaudio/default.pa#L7-L20)
- [init_identity.py:180-184](file://docker/ts3client/init_identity.py#L180-L184)

## 跨平台音频处理

### 平台检测与兼容性
系统通过 `platform.system() == "Darwin"` 进行平台检测，实现以下跨平台兼容性：

- **AudioController**：检测平台并在 Linux 上执行音量输入刷新，在 macOS 上跳过
- **FFmpegProcess**：根据平台选择不同的输出格式
  - Linux：`-f pulse` 输出到 ts3bot_music sink
  - macOS：`-f audiotoolbox` 输出到系统 Core Audio
- **VolumeController**：根据平台决定音量控制策略
  - Linux：支持两层音量控制（FFmpeg + pactl）
  - macOS：仅支持 FFmpeg 粗调音量

### 条件性 sink 输入刷新
在 Linux 平台上，AudioController 在播放开始后会执行以下流程：
1. 启动 FFmpeg 进程
2. 等待 0.5 秒让 FFmpeg 完全启动
3. 调用 VolumeController.refresh_sink_input() 刷新 PulseAudio sink 输入
4. 应用当前音量设置

在 macOS 平台上，由于使用 Core Audio 输出，系统会跳过此流程。

### 双sink架构支持
**重要更新**：AudioController 现在支持 ts3bot_music sink，为双sink架构做准备：

- **配置支持**：AudioController 构造函数接受 pulse_sink 参数，默认为 "ts3bot_music"
- **兼容性**：保持与单sink架构的兼容性，可通过配置切换
- **未来扩展**：为后续的音频隔离功能提供基础

**章节来源**
- [controller.py:87-91](file://bot/core/audio/controller.py#L87-L91)
- [ffmpeg.py:88-96](file://bot/core/audio/ffmpeg.py#L88-L96)
- [volume.py:84-120](file://bot/core/audio/volume.py#L84-L120)
- [config.yaml:14-18](file://config/config.yaml#L14-L18)
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)

## 音频质量验证系统

### ffprobe集成与质量验证
YtDlpService 现在集成了ffprobe进行音频文件质量验证，确保下载的音频文件符合预期：

- **实际时长验证**：使用 `_ffprobe_duration()` 方法获取文件的实际播放时长
- **预期时长对比**：比较实际时长与元数据中的预期时长，识别截断或低质量文件
- **质量阈值设置**：当实际时长小于预期时长的50%时，判定为低质量文件
- **文件大小验证**：确保下载文件至少10KB，避免损坏或空文件
- **日志记录**：详细记录验证过程和结果，便于调试和监控

### 验证流程
```mermaid
flowchart TD
Download["YtDlpService.download_audio()"] --> TempFile["生成临时文件<br/>path: /tmp/ts3bot_*.ext"]
TempFile --> FFProbe["调用 _ffprobe_duration()"]
FFProbe --> Compare["比较实际时长与预期时长"]
Compare --> |实际时长 < 预期时长 * 0.5| Reject["拒绝文件：截断或低质量"]
Compare --> |满足质量要求| Validate["验证文件大小 >= 10KB"]
Validate --> |文件过小| Reject
Validate --> |通过验证| Accept["接受文件：质量合格"]
Reject --> LogError["记录错误日志"]
Accept --> Return["返回 DownloadedAudio 对象"]
LogError --> Return
```

**图表来源**
- [ytdlp.py:151-173](file://bot/services/netease/ytdlp.py#L151-L173)
- [ytdlp.py:200-221](file://bot/services/netease/ytdlp.py#L200-L221)

### 验证阈值与算法
- **时长验证阈值**：实际时长必须大于预期时长的50%，避免播放时间过短的预览版本
- **文件大小阈值**：至少10KB，防止空文件或损坏文件被播放
- **日志记录**：详细记录验证过程，包括文件大小、实际时长、预期时长等信息
- **错误处理**：验证失败时返回None，避免播放低质量音频

**章节来源**
- [ytdlp.py:151-173](file://bot/services/netease/ytdlp.py#L151-L173)
- [ytdlp.py:200-221](file://bot/services/netease/ytdlp.py#L200-L221)

## PulseAudio路由机制改进

### 路由机制概述
PulseAudio路由机制经过重大改进，从原来的单sink架构改为双sink分离架构，提升了音频路由的正确性和稳定性：

- **双sink架构**：通过 `docker/pulseaudio/default.pa` 创建 ts3bot_music 和 ts3bot_playback 两个独立的虚拟音频设备
- **默认设备设置**：将 ts3bot_music 设置为默认音频输出设备，ts3bot_music.monitor 设置为默认音频输入设备
- **TS3客户端配置**：通过 `docker/ts3client/init_identity.py` 配置TS3客户端使用双sink架构
- **容器环境适配**：通过 `docker/entrypoint.sh` 确保PulseAudio在Docker容器中正确启动和配置

### 路由配置详解
```mermaid
flowchart TD
Start["系统启动"] --> PulseAudio["启动 PulseAudio 服务"]
PulseAudio --> Config["加载 default.pa 配置"]
Config --> CreateMusicSink["创建 ts3bot_music 虚拟设备<br/>rate=48000, format=s16le, channels=2"]
CreateMusicSink --> CreatePlaybackSink["创建 ts3bot_playback 虚拟设备<br/>rate=48000, format=s16le, channels=2"]
CreatePlaybackSink --> SetDefaults["设置默认设备<br/>默认sink: ts3bot_music<br/>默认source: ts3bot_music.monitor"]
SetDefaults --> TS3Config["配置 TS3 客户端音频设置<br/>Capture: ts3bot_music.monitor<br/>Playback: ts3bot_playback"]
TS3Config --> BotAudio["配置 Bot 音频输出"]
BotAudio --> Route["建立完整的音频路由链路"]
Route --> Test["测试音频输出"]
Test --> Success["音频路由正常"]
```

**图表来源**
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)
- [entrypoint.sh:38-79](file://docker/entrypoint.sh#L38-L79)

### 配置文件分析
- **default.pa**：创建两个独立的null-sink模块，设置默认设备、加载本地协议模块
- **init_identity.py**：配置TS3客户端使用双sink架构，捕获从 ts3bot_music.monitor 输出的音频
- **entrypoint.sh**：确保PulseAudio服务正确启动，设置PULSE_SERVER环境变量

**章节来源**
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)
- [entrypoint.sh:38-79](file://docker/entrypoint.sh#L38-L79)

## 持续时间参数与提前退出检测

### 持续时间参数支持
AudioController 和 FFmpegProcess 现在支持持续时间参数，主要用于以下场景：

- **预期时长检测**：play() 方法新增 duration 参数，表示预期播放时长（秒）
- **提前退出检测**：FFmpegProcess 在监控线程中检查播放进度与预期时长的关系
- **参数传递**：music.py 处理器从 YtDlpService 获取的 DownloadedAudio 对象中提取持续时间信息

### 提前退出检测机制
FFmpegProcess 实现了基于预期时长的提前退出检测逻辑：

- **检测条件**：当预期时长大于 10 秒，且实际播放进度小于预期时长的 80%
- **检测时机**：在播放结束后检查，避免误判短音频
- **日志记录**：记录详细的提前退出信息，包括进度、预期时长、实际耗时和最后 stderr 输出
- **阈值设置**：80% 阈值用于区分网络中断和音频播放完成

### 与 YtDlpService 的集成
YtDlpService 生成的临时文件包含持续时间信息，通过以下方式传递给 AudioController：

- **下载过程**：YtDlpService.download_audio() 将音频下载到临时文件
- **持续时间提取**：DownloadedAudio 数据类包含 duration 字段
- **文件路径传递**：music.py 处理器将下载得到的文件路径和持续时间作为参数传递给 AudioController.play()
- **生命周期管理**：AudioController 负责在播放完成后自动清理这些临时文件

```mermaid
flowchart TD
Download["YtDlpService.download_audio()"] --> TempFile["生成临时文件<br/>path: /tmp/ts3bot_*.ext"]
TempFile --> ExtractDuration["提取持续时间信息"]
ExtractDuration --> PassParams["传递 (source, temp_file, duration)"]
PassParams --> PlayCall["AudioController.play(source, temp_file, duration)"]
PlayCall --> StartPlayback["开始播放"]
StartPlayback --> Monitor["监控播放进度"]
Monitor --> CheckPremature{"提前退出检测"}
CheckPremature --> |是| LogWarning["记录提前退出警告"]
CheckPremature --> |否| NormalEnd["正常结束"]
NormalEnd --> Cleanup["播放结束/错误/停止"]
Cleanup --> DeleteFile["os.unlink(temp_file)"]
DeleteFile --> Log["记录清理日志"]
```

**图表来源**
- [ytdlp.py:171-177](file://bot/services/netease/ytdlp.py#L171-L177)
- [music.py:57-63](file://bot/core/commands/handlers/music.py#L57-L63)
- [controller.py:76-118](file://bot/core/audio/controller.py#L76-L118)
- [ffmpeg.py:268-273](file://bot/core/audio/ffmpeg.py#L268-L273)

**章节来源**
- [controller.py:76-82](file://bot/core/audio/controller.py#L76-L82)
- [controller.py:106-110](file://bot/core/audio/controller.py#L106-L110)
- [ffmpeg.py:98-113](file://bot/core/audio/ffmpeg.py#L98-L113)
- [ffmpeg.py:268-273](file://bot/core/audio/ffmpeg.py#L268-L273)
- [ytdlp.py:171-177](file://bot/services/netease/ytdlp.py#L171-L177)
- [music.py:57-63](file://bot/core/commands/handlers/music.py#L57-L63)

## 临时文件管理与自动清理

### 临时文件管理机制
AudioController 现在支持临时文件的自动管理，主要通过以下机制实现：

- **临时文件跟踪**：`_current_temp_file` 字段记录当前正在播放的临时文件路径
- **自动清理策略**：
  - 播放结束（EOF）时自动清理临时文件
  - 播放错误时自动清理临时文件  
  - 手动停止播放时自动清理临时文件
  - 新播放开始前清理上一个临时文件
- **清理实现**：`_cleanup_temp_file()` 方法负责删除临时文件并处理可能的OSError异常

### 与 YtDlpService 的集成
YtDlpService 生成的临时文件通过以下方式传递给 AudioController：

- **下载过程**：YtDlpService.download_audio() 将音频下载到临时文件
- **文件路径传递**：music.py 处理器将下载得到的文件路径作为 `temp_file` 参数传递给 AudioController.play()
- **生命周期管理**：AudioController 负责在播放完成后自动清理这些临时文件

### 临时文件的使用场景
- **CDN URL 过期问题**：许多平台的音频URL会在播放过程中过期，使用临时文件可以避免这个问题
- **网络不稳定**：本地文件播放更加稳定，不受网络波动影响
- **缓存策略**：结合 TTLCache 实现智能缓存，避免重复下载相同内容
- **音频质量验证**：临时文件在播放前经过ffprobe质量验证，确保只有合格的音频文件被播放

```mermaid
flowchart TD
Download["YtDlpService.download_audio()"] --> TempFile["生成临时文件<br/>path: /tmp/ts3bot_*.ext"]
TempFile --> QualityCheck["ffprobe质量验证"]
QualityCheck --> PassCheck{"验证通过？"}
PassCheck --> |是| PassParam["传递 temp_file 参数"]
PassCheck --> |否| Reject["拒绝文件并清理"]
PassParam --> PlayCall["AudioController.play(source, temp_file)"]
PlayCall --> StartPlayback["开始播放"]
StartPlayback --> Cleanup["播放结束/错误/停止"]
Cleanup --> DeleteFile["os.unlink(temp_file)"]
DeleteFile --> Log["记录清理日志"]
```

**图表来源**
- [ytdlp.py:82-177](file://bot/services/netease/ytdlp.py#L82-L177)
- [controller.py:76-187](file://bot/core/audio/controller.py#L76-L187)

**章节来源**
- [controller.py:52-53](file://bot/core/audio/controller.py#L52-L53)
- [controller.py:76-98](file://bot/core/audio/controller.py#L76-L98)
- [controller.py:119-187](file://bot/core/audio/controller.py#L119-L187)
- [ytdlp.py:82-177](file://bot/services/netease/ytdlp.py#L82-L177)

## 依赖关系分析
- **AudioController** 依赖 FFmpegProcess 与 VolumeController
- **BotApplication** 依赖 AudioController，并通过 set_callbacks 注入播放完成/错误回调
- **命令处理器 music.py** 依赖 BotApplication 的 audio 实例和 YtDlpService
- **YtDlpService** 依赖 yt_dlp 库和临时文件系统，提供持续时间信息
- **TTLCache** 提供内存缓存支持
- **配置 config.py** 和 **config.yaml** 提供音频相关参数，影响 AudioController 初始化
- **default.pa** 提供双sink分离配置，支持 ts3bot_music 和 ts3bot_playback 设备
- **init_identity.py** 配置TS3客户端使用双sink架构
- **entrypoint.sh** 确保PulseAudio服务正确启动和配置

```mermaid
graph LR
CFG["config.py"] --> APP["app.py"]
YAML["config.yaml"] --> CFG
APP --> AC["controller.py"]
AC --> FFM["ffmpeg.py"]
AC --> VOL["volume.py"]
APP --> CMD["music.py"]
CMD --> AC
CMD --> YTDL["ytdlp.py"]
YTDL --> CACHE["cache.py"]
AC -.->|"平台检测"| PLATFORM["platform.system() == 'Darwin'"]
YAML -.->|"pulse_sink_name"| PA["default.pa"]
PA -.->|"TS3客户端配置"| TS3["init_identity.py"]
TS3 -.->|"PulseAudio启动"| ENTRY["entrypoint.sh"]
```

**图表来源**
- [config.py:48-55](file://bot/config.py#L48-L55)
- [app.py:54-59](file://bot/app.py#L54-L59)
- [controller.py:10-11](file://bot/core/audio/controller.py#L10-L11)
- [music.py:20-93](file://bot/core/commands/handlers/music.py#L20-L93)
- [ytdlp.py:44-177](file://bot/services/netease/ytdlp.py#L44-177)
- [config.yaml:14-18](file://config/config.yaml#L14-L18)
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)
- [entrypoint.sh:38-79](file://docker/entrypoint.sh#L38-L79)

**章节来源**
- [controller.py:10-11](file://bot/core/audio/controller.py#L10-L11)
- [app.py:54-59](file://bot/app.py#L54-L59)
- [music.py:20-93](file://bot/core/commands/handlers/music.py#L20-L93)
- [ytdlp.py:44-177](file://bot/services/netease/ytdlp.py#L44-177)
- [config.yaml:14-18](file://config/config.yaml#L14-L18)
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)
- [entrypoint.sh:38-79](file://docker/entrypoint.sh#L38-L79)

## 性能考虑
- **子进程与 I/O 非阻塞**：AudioController 与 FFmpegProcess 均采用 asyncio，避免阻塞事件循环
- **音量控制平滑**：VolumeController 使用固定步长与步进延迟进行淡入淡出，降低突变噪声
- **启动后延时**：play() 后短暂 sleep 再刷新音量输入，确保 PulseAudio sink-input 就绪（Linux 专用）
- **重连参数**：FFmpegProcess 启动时启用流式重连参数，提升网络不稳定场景下的鲁棒性
- **资源回收**：stop() 显式取消监控任务并终止/杀死进程，防止僵尸进程
- **关机安全**：BotApplication.stop() 调用 fade_out_and_stop()，先淡出再停止，避免突停
- **平台优化**：macOS 平台跳过不必要的音量输入刷新，减少系统调用开销
- **双sink优化**：**新增** 双sink架构确保音频完全隔离，避免相互干扰
- **音频质量优化**：**新增** 500ms分析窗口和0.9峰值目标提升音频质量
- **延迟优化**：**新增** 播放前0.5秒延迟确保FFmpeg完全启动，避免音量控制失败
- **临时文件优化**：**新增** 自动清理机制避免磁盘空间浪费，结合缓存策略提升I/O性能
- **内存缓存优化**：**新增** TTLCache 提供智能缓存，减少重复下载和网络请求
- **持续时间优化**：**新增** 基于预期时长的提前退出检测，避免无效播放和资源浪费
- **自动清理优化**：**新增** 所有状态转换的自动清理机制，确保资源及时释放
- **PulseAudio路由优化**：**新增** 双sink架构确保音频正确路由到目标设备

**章节来源**
- [controller.py:86-87](file://bot/core/audio/controller.py#L86-L87)
- [volume.py:46-59](file://bot/core/audio/volume.py#L46-L59)
- [ffmpeg.py:66-80](file://bot/core/audio/ffmpeg.py#L66-L80)
- [ffmpeg.py:94-115](file://bot/core/audio/ffmpeg.py#L94-L115)
- [app.py:314-315](file://bot/app.py#L314-L315)
- [ytdlp.py:82-177](file://bot/services/netease/ytdlp.py#L82-L177)
- [cache.py:9-46](file://bot/services/netease/cache.py#L9-L46)

## 故障排查指南
- **无法播放/黑屏无声**
  - 检查 PulseAudio 源名与 FFmpeg 输出 sink 是否一致（Linux 专用）
  - 确认 FFmpeg 可执行路径与权限
  - 观察 FFmpeg stderr 日志，确认是否出现连接错误或格式不支持
  - **新增**：检查系统平台是否为 macOS，确认使用 Core Audio 输出
  - **新增**：验证 ts3bot_music 和 ts3bot_playback 两个sink是否正确创建和配置
  - **新增**：检查播放前延迟是否足够，确保FFmpeg完全启动
  - **新增**：确认TS3客户端音频设置是否正确配置为双sink架构
  - **新增**：验证PulseAudio服务是否在Docker容器中正确启动
- **播放卡住或无法停止**
  - 确认 stop() 是否被调用且监控任务已取消
  - 检查进程是否被 SIGSTOP（pause）冻结
- **音量异常**
  - 确认 pactl 是否可用，sink-input 是否成功刷新（Linux 专用）
  - 检查音量范围与淡入时长配置
  - **新增**：在 macOS 平台上，音量控制仅限于 FFmpeg 粗调
  - **新增**：检查 ts3bot_music sink 的音量控制是否正常
- **回调未触发**
  - 确认 BotApplication 是否在初始化时注册了 on_stopped/on_error
  - 检查 FFmpeg 退出码是否为 EOF 或错误码
- **跨平台问题**
  - **Linux**：确认 PulseAudio 服务正常运行，ts3bot_music sink 设备可用
  - **macOS**：确认系统 Core Audio 可用，检查音频输出设备设置
- **路由问题**
  - **新增**：检查 PulseAudio 配置文件 `default.pa` 是否正确加载
  - **新增**：验证 ts3bot_music 和 ts3bot_playback 虚拟音频设备是否创建成功
  - **新增**：确认 FFmpeg 命令中的 `-f pulse` 和 `-sink_name` 参数正确
  - **新增**：检查TS3客户端是否使用双sink架构配置
  - **新增**：验证entrypoint.sh是否正确启动PulseAudio服务
- **延迟问题**
  - **新增**：如果音量控制立即生效但失败，检查播放前延迟是否足够
  - **新增**：确认0.5秒延迟后是否正确刷新音量输入
- **临时文件问题**
  - **新增**：检查临时文件是否正确生成和传递
  - **新增**：确认临时文件清理机制是否正常工作
  - **新增**：验证磁盘空间是否充足，避免临时文件过多占用空间
  - **新增**：检查文件权限，确保可以读取和删除临时文件
- **持续时间检测问题**
  - **新增**：检查 YtDlpService 是否正确提取音频持续时间
  - **新增**：确认 music.py 处理器是否正确传递持续时间参数
  - **新增**：验证提前退出检测逻辑是否按预期工作
  - **新增**：检查日志中是否正确记录提前退出警告信息
- **音频质量优化问题**
  - **新增**：检查动态音频规范化滤镜链参数是否正确应用
  - **新增**：验证500ms分析窗口和0.9峰值目标是否生效
  - **新增**：确认音频质量提升效果是否符合预期
- **双sink架构问题**
  - **新增**：检查 ts3bot_music 和 ts3bot_playback 两个sink是否正确分离
  - **新增**：验证音频隔离是否正常，回放音频不应被机器人捕获
  - **新增**：确认 feedback loop 是否完全消除
  - **新增**：检查音频路由是否正确指向目标sink

**章节来源**
- [ffmpeg.py:127-177](file://bot/core/audio/ffmpeg.py#L127-L177)
- [volume.py:77-108](file://bot/core/audio/volume.py#L77-L108)
- [app.py:107-110](file://bot/app.py#L107-L110)
- [controller.py:177-187](file://bot/core/audio/controller.py#L177-L187)
- [ytdlp.py:82-177](file://bot/services/netease/ytdlp.py#L82-L177)
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)
- [entrypoint.sh:38-79](file://docker/entrypoint.sh#L38-L79)

## 结论
AudioController 通过清晰的状态机与回调机制，将 FFmpeg 子进程与 PulseAudio 音量控制解耦，实现了稳定、可扩展的音频播放能力。**核心架构变更**将单sink双用途架构升级为双sink分离架构，通过 ts3bot_music 和 ts3bot_playback 两个独立的sink实现完全的音频隔离，完全消除了音频反馈循环问题。**音频质量优化**将动态音频规范化滤镜链参数从150ms分析窗口和0.95峰值目标优化为500ms分析窗口和0.9峰值目标，显著提升了音频质量。**新增的双sink架构**确保了音乐输出和回放音频的完全分离，提供了更稳定、更高质量的音频播放体验。**新增的音频质量优化**通过更长的分析窗口和平滑的峰值目标，减少了音频压缩感，提供了更自然的听觉体验。**新增的自动清理机制**确保了播放完成后临时文件的及时释放，避免了磁盘空间浪费。结合 BotApplication 的自动播放与优雅关机策略，整体系统具备良好的用户体验与健壮性。建议在生产环境中关注日志级别、网络稳定性、资源回收以及双sink架构的特定配置，特别是 ts3bot_music 和 ts3bot_playback sink 的正确配置、临时文件管理策略、持续时间检测机制和音频质量优化系统，以获得更佳的运行表现。

## 附录
- **配置项参考**（来自 config.py 和 config.yaml）：
  - 音频设备：pulse_sink_name（默认："ts3bot_music"）
  - 默认音量：default_volume（默认：70）
  - 淡入时长：fade_duration_ms（默认：500）
  - FFmpeg 路径：ffmpeg_path（默认：自动检测）
  - 缓存目录：cache_dir（默认："/data/cache"）
  - 音频质量：audio_quality（默认："exhigh"）
- **常见命令**（来自 music.py）：
  - !play：点歌/解析链接并播放
  - !pause：暂停
  - !resume：继续
  - !stop：停止（管理员）
- **平台特定信息**：
  - **Linux**：使用 PulseAudio 输出，支持完整的音量控制
  - **macOS**：使用 Core Audio 输出，音量控制受限于系统限制
- **PulseAudio 配置**（来自 default.pa）：
  - 创建 ts3bot_music 和 ts3bot_playback 两个独立的虚拟音频设备
  - 设置默认音频输出为 ts3bot_music，输入为 ts3bot_music.monitor
  - 配置本地协议支持
  - 禁用自动挂起以适配容器环境
- **TS3客户端配置**（来自 init_identity.py）：
  - 配置TS3客户端使用双sink架构
  - 捕获设备：ts3bot_music.monitor
  - 回放设备：ts3bot_playback
  - 启用自动连接和捕获
- **PulseAudio启动脚本**（来自 entrypoint.sh）：
  - 启动PulseAudio服务（用户模式）
  - 设置PULSE_SERVER环境变量
  - 验证PulseAudio服务状态
  - 配置容器环境变量
- **新增特性**：
  - **双sink分离架构**：完全消除音频反馈循环问题
  - **音频质量优化**：500ms分析窗口和0.9峰值目标
  - **动态音频规范化优化**：提升音频质量和稳定性
  - **音频隔离增强**：音乐输出和回放音频完全分离
  - **改进的音频路由**：双sink架构确保音频正确路由
  - **TS3客户端双sink配置**：支持双sink架构的音频设置

**章节来源**
- [config.py:48-55](file://bot/config.py#L48-L55)
- [config.yaml:14-18](file://config/config.yaml#L14-L18)
- [music.py:20-93](file://bot/core/commands/handlers/music.py#L20-L93)
- [default.pa:1-34](file://docker/pulseaudio/default.pa#L1-L34)
- [init_identity.py:177-212](file://docker/ts3client/init_identity.py#L177-L212)
- [entrypoint.sh:38-79](file://docker/entrypoint.sh#L38-L79)
- [ytdlp.py:82-177](file://bot/services/netease/ytdlp.py#L82-L177)
- [cache.py:9-46](file://bot/services/netease/cache.py#L9-L46)